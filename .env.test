DATABASE_URL="postgresql://postgres:Gracy12345@localhost:5432/pharmeasy?schema=public"
JWT_SECRET="szfdxgcvhjbkvivsubhrvukvrhybyerbvlaebvahyebvrialeribverbvaeruv"
SCHEDULER_SECRET="ec2965ac85e6cee59bc7eae2d059050f40438ce4bee014c664c3aaa28550a4e7"

# Dummy values only — real sendMail() calls are jest.mock'd in the test
# suite, so these never hit the real Brevo API. They exist purely so a
# test that forgets to mock @/lib/mailer fails loudly instead of throwing
# a confusing "not configured" error.
BREVO_API_KEY="test-dummy-brevo-key"
MAIL="test@pharmeasy.local"